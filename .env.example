# MongoDB Atlas Connection Variables (Option 1: Individual variables)
DB_USER=your-username
DB_PASSWORD=your-password
DB_CLUSTER=your-cluster.mongodb.net
DB_NAME=your-database-name

# OR MongoDB Atlas Full Connection String (Option 2: Single variable)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Server Port (Heroku will set this automatically)
PORT=3000

# Node Environment
NODE_ENV=production

# Optional: Logger settings for production
LOG_LEVEL=info
CONSOLE_OUTPUT=off